[package]
name = "safety-guard-v2"
version = "0.2.0"
description = "Non-custodial trip guarding commitments and reputation on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "safety_guard_v2"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = { version = "=0.32.1", features = ["init-if-needed"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
